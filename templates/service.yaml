apiVersion: v1
kind: Service
metadata:
  name: {{$.Values.name}}-service
spec:
  type: LoadBalancer
  ports:
  - port: {{default "8000" $.Values.port}}
    targetPort: {{default "8000" $.Values.port}}
    protocol: TCP
  selector:
    app: {{$.Values.name}}
